package com.br.caring.saude.api.sgi.domain.enums;

/*
 * REMOVIDO: o enum `Perfil` foi removido do código-base.
 * Use a entidade `com.br.caring.saude.api.sgi.domain.entity.Perfil` (tabela `perfis`) em vez deste enum.
 *
 * Mantive o arquivo apenas como placeholder/comment para o histórico; nenhuma classe/enum é declarado aqui.
 * Se algum código legado ainda referenciar `domain.enums.Perfil`, restaure o enum ou atualize as referências.
 */
